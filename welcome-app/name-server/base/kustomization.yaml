apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

nameSuffix: -name-server

labels:
  - pairs:
      app: welcome-app
      service: name-server
    includeSelectors: true
    includeTemplates: true

resources:
  - deployment.yaml

replicas:
  - name: welcome-app
    count: 1
